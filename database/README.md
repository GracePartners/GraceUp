# Database

PostgreSQL schema and migration scaffolding for GraceUp CRM.

- `migrations/`: ordered migration files
- `schema/`: base SQL definitions grouped by domain
- `seeds/`: development/test seed data
- `docs/`: ERD and migration notes
